# Selected by `vite --mode webserver` (dev) and `vite build --mode webserver` (prod).
# Routes the studio at the remote axum engine instead of the in-process wasm engine.
VITE_BACKEND=http
VITE_API_URL=/api
