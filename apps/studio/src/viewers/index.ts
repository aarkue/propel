// Viewer registry contract + return-type resolver (studio-internal).
//
// Per-viewer adapters (dfg.ts, petri-net.ts, ...) are auto-collected via `import.meta.glob` in
// panels/viewer-registry.ts.
export * from "./registry";
